# NOTE: Type forced to be:
# trs2 - TRS-80 Level II BASIC tokenized file
   10 CLS 
   20 PRINT @283,"3-D PLOT"
   60 FOR QZ = 1 TO 1000
   70 NEXT QZ
   80 CLS 
   90 PRINT "DO YOU HAVE A PRINTER";
  100 INPUT A$ : IF LEFT$ (A$,1) = "Y" THEN 120
  110 GOTO 160
  120 Q = 1
  130 PRINT "TURN ON YOUR PRINTER AND HIT ANY KEY"
  140 A$ = INKEY$ : IF A$ = ""THEN 140
  150 GOTO 190
  160 Q = 0
  170 GOTO 190
  180 PRINT "THERE ARE ONLY SIX FUNCTIONS, ONE THROUGH SIX INCLUSIVE"
  190 CLS : PRINT "WHICH FUNCTION WOULD YOU LIKE TO HAVE ME PRINT <1-6>"
  200 INPUT A : IF A > 6 OR A < 1 THEN 180
  210 CLS 
  220 FOR X = - 30 TO 30 STEP 1.5
  230 L = 0
  240 Y1 = 5 * INT ( SQR (900 - X * X) / 5)
  250 FOR Y = Y1 TO - Y1 STEP - 5
  260 P = SQR (X * X + Y * Y)
  270 GOSUB 390
  280 IF Z < = L THEN 330
  290 L = Z
  300 IF Q = 0 THEN 320
  310 LPRINT TAB Z);"*";
  320 PRINT TAB Z);"*";
  330 NEXT Y
  340 IF Q = 0 THEN 360
  350 LPRINT 
  360 PRINT 
  370 NEXT X
  380 GOTO 520
  390 ON A GOTO 400,420,440,460,480,500
  400 Z = INT (25 + (30 * CSNG ( - P * P / 100)) - .7 * Y)
  410 GOTO 510
  420 Z = INT (25 + ( SQR (900.01 - P * P) * .9 - 2) - .7 * Y)
  430 GOTO 510
  440 Z = INT (25 + (30 * ( COS (P / 16) ^ 2)) - .7 * Y)
  450 GOTO 510
  460 Z = INT (25 + (30 - 30 * SIN (P / 19)) - .7 * Y)
  470 GOTO 510
  480 Z = INT (25 + (30 * CSNG ( - COS (P / 16)) - 30) - .7 * Y)
  490 GOTO 510
  500 Z = INT (25 + (30 * SIN (P / 10)) - .7 * Y)
  510 RETURN 
  520 PRINT "WOULD YOU LIKE ME TO PRINT ANOTHER FUNCTION";
  530 INPUT B$ : IF LEFT$ (B$,1) = "Y" THEN 190
  540 END 
